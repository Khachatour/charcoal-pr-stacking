"""Placeholder test to ensure pytest passes during project setup."""


def test_placeholder() -> None:
    """Placeholder test that always passes.

    This test exists to ensure the test suite passes during initial project
    setup. It should be removed once actual tests are added.
    """
    assert True
